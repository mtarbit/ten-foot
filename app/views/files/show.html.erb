<% if @file.use_vlc? %>

    <object type="application/x-vlc-plugin" toolbar="no" data-duration="<%= @file.duration %>">
        <param name="src" value="<%= @file.url %>">
    </object>

<% else %>

    <video autoplay>
        <source src="<%= @file.url %>"></source>
    </video>

<% end %>
